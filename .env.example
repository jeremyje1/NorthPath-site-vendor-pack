# Public brand metadata
NEXT_PUBLIC_BRAND_NAME="NorthPath Strategies"
NEXT_PUBLIC_CONTACT_EMAIL="jeremy.estrella@gmail.com"

# Optional: Payment Link IDs (use these if using client-side payment links instead of server Checkout sessions)
NEXT_PUBLIC_PAYMENT_LINK_VENDOR_PACK=""
NEXT_PUBLIC_PAYMENT_LINK_VENDOR_PACK_UPDATES=""
NEXT_PUBLIC_PAYMENT_LINK_CONNECTORS=""
NEXT_PUBLIC_PAYMENT_LINK_EVALOPS=""

# Stripe secret key (server)
STRIPE_SECRET_KEY="sk_test_..."

# Stripe Price IDs (for creating Checkout Sessions / subscriptions)
STRIPE_PRICE_VENDOR_PACK_ONE_TIME="price_..."
STRIPE_PRICE_VENDOR_PACK_UPDATES_MONTHLY="price_..."
STRIPE_PRICE_VENDOR_PACK_UPDATES_ANNUAL="price_..."
STRIPE_PRICE_CONNECTORS_SINGLE_MONTHLY="price_..."
STRIPE_PRICE_CONNECTORS_ALL_MONTHLY="price_..."
STRIPE_PRICE_CONNECTORS_ALL_ANNUAL="price_..."
STRIPE_PRICE_EVALOPS_MONTHLY="price_..."
STRIPE_PRICE_EVALOPS_ANNUAL="price_..."
STRIPE_PRICE_BUNDLE_MONTHLY="price_..."
STRIPE_PRICE_BUNDLE_ANNUAL="price_..."
STRIPE_PRICE_ENTERPRISE_MONTHLY="price_..."
STRIPE_PRICE_ENTERPRISE_ANNUAL="price_..."

# Redirect URLs after checkout
NEXT_PUBLIC_CHECKOUT_SUCCESS_URL="http://localhost:3000/success"
NEXT_PUBLIC_CHECKOUT_CANCEL_URL="http://localhost:3000/cancel"

# Stripe Webhook secret (from stripe listen or dashboard endpoint)
STRIPE_WEBHOOK_SECRET="whsec_..."

# AI Gateway (server-only)
AI_GATEWAY_API_KEY="your_ai_gateway_api_key_here"

# Notes:
# - Never commit a real STRIPE_SECRET_KEY. Use this example file for reference.
# - Stripe signature verification requires the raw request body.
#   In Next.js Route Handlers capture the raw body (e.g., using `req.text()` before any JSON parsing)
#   and pass it along with the signature header to `stripe.webhooks.constructEvent`.
